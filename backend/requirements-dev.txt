# Test-only dependencies -- split out of requirements.txt so the production
# Docker image (backend/Dockerfile) doesn't carry a test runner and an HTTP
# client library it never uses at runtime. Install alongside requirements.txt
# for local development and CI: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==8.3.3
httpx==0.28.1  # FastAPI's TestClient needs this at test time only
